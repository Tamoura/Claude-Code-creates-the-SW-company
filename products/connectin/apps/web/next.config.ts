import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Use port 3111 as assigned in PORT-REGISTRY.md
  // Start with: npm run dev -- -p 3111
  // Or set in .env.local: PORT=3111
};

export default nextConfig;
