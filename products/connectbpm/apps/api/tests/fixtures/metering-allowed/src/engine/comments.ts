/* eslint-disable */
// FIXTURE — prose, not code.
// A comment mentioning UsageService, or prisma.usageEvent.create(...), or
// new Function('...'), is documentation. The gate must not flag it, otherwise
// nobody can write down WHY the rule exists without tripping the rule.
export const noop = (): undefined => undefined;
