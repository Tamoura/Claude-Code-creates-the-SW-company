import type { PricingMetadata } from '../types';

/**
 * Pricing data metadata
 * Update this when pricing data is refreshed
 */
export const pricingMetadata: PricingMetadata = {
  lastUpdated: '2025-01-25',
  version: '1.0.0',
  currency: 'USD',
  pricingType: 'on-demand',
};
