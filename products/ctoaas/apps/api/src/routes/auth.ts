import { FastifyPluginAsync } from 'fastify';

/**
 * Auth routes stub — will be expanded in IMPL-010.
 */
const authRoutes: FastifyPluginAsync =
  async (_fastify) => {
    // Routes will be registered here in IMPL-010
  };

export default authRoutes;
