# AIRouter API Configuration
PORT=5006
NODE_ENV=development

# Database
DATABASE_URL=postgresql://postgres@localhost:5432/airouter_dev

# JWT
JWT_SECRET=dev-jwt-secret-change-in-production

# Provider Key Encryption (AES-256-GCM)
# Generate with: openssl rand -hex 32
PROVIDER_KEY_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# API Key HMAC Secret (optional in dev, required in production)
# Generate with: openssl rand -hex 64
# API_KEY_HMAC_SECRET=

# Redis (optional - rate limiting uses in-memory if not set)
# REDIS_URL=redis://localhost:6379

# Allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3110
